<?php
	/** main file for plugins **/
	// This is only an example.

	/* $userData = plugin::data()->getUserByName('putSomeNameHere');

	if ($userData instanceof UserData) {
		$content = $userData->getUsername() . ' is ' . ($userData->isAdmin() ? 'admin' : 'something else') . '.';
	} else {
		$content = 'not found';
	}

	plugin::TPL()->addToArea('aboveContent', $content);

	plugin::dataWritable()->updateUserByID($userData->getUserID(), new UserDataWritable(
		$userData->getUsername(),
		$userData->getAvatar(),
		$userData->getEmailAdress(),
		$userData->getLevel(),
		$userData->getSignature(),
		$userData->getStatus(),
		$userData->getPoints(),
		$userData->isBanned(),
		$userData->getWebsite(),
		$userData->getSkype()
	)); */
?>